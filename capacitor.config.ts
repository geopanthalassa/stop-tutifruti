import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.nuku.stoptutifruti",
  appName: "Stop / Tutifruti",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
